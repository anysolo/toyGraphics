package com.anysolo.toyGraphics.samples

import com.anysolo.toyGraphics.*

/*
fun main() {
    val wnd = Window(800, 600)
    val keyboard = Keyboard(wnd, eventMode = true)

    while(true) {
        while(true) {
            val event = keyboard.getEvent() ?: break

            println(event)
        }

        sleep(20)
    }
}
*/
